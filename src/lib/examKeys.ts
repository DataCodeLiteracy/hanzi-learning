export const selectedHanziKey = (grade: number) =>
  `exam_selected_hanzi_${grade}`
export const resultKey = (examId: string) => `exam_result_${examId}`
export const experienceAppliedKey = (examId: string) =>
  `exam_experience_applied_${examId}`
